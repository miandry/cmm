mz_visitor.settings:
  type: config_object
  label: 'Visitor User Settings'
  mapping:
    default_role:
      type: string
      label: 'Default Role'
    expiration_duration:
      type: integer
      label: 'Expiration Duration (Days)'
